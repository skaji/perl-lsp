package DL::Doc;

use DL::Target;

# tracker: https://example.com/tracker/42 (see notes)

sub run { return DL::Target::hello() }

=head1 SEE ALSO

L<DL::Target> is the sibling module, L<No::Such::Module> is not installed,
and the spec lives at L<https://example.com/spec>.

=cut

1;
