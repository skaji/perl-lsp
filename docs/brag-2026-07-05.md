# The brag list — tightening round → round 3 (2026-07-05)

Running tally of wins while you were out. Newest at the bottom of each
section; numbers are real-corpus, not synthetic.

## Landed

- **Completion gathering is now one vocabulary end-to-end** (wave 2):
  every entity-content source — Perl import lists, module names,
  qualified-path drills, members, hash keys, pack members — yields
  `CompletionCandidate` and flows through ONE adapter projection.
  `symbols.rs` shed 160 lines and got thinner, exactly what rule #3 wants.
  Proven byte-identical by an exhaustive sweep: 538 cursor positions
  across Perl and cpp fixtures, zero diffs.
- **Your op.c `refcount` report: FIXED, and the hunt was a saga.** The
  trail went typedef-hop → pTHX_ macro chains → (opus bisection) the REAL
  bug, two layers deeper than anyone guessed: an uppercase macro call
  (`RCPVx(pv)`) mis-fires the ctor-convention heuristic, and the bogus
  flow witness was SHADOWING the explicitly declared type on a
  latest-wins tie-break. The fix is one honest lattice rule — an explicit
  annotation outranks a flow guess — no macro names, no special cases.
  Verified on your exact coordinates: `rcpv->refcount` → cop.h:574:13,
  deterministic; `o->op_type` in Perl_op_free → opnames.h; hover on
  `Perl_op_free` answers where it used to shrug. Gold 342, both xfails
  promoted + a hover row born gold. Bonus finds bagged en route: the
  cold-start flake caught red-handed poisoning probe evidence, a
  workspace-vs-open-doc parity gap, and two fragile heuristics ledgered
  with bisection proof.
- **cpp chains no longer die at `auto`** — both parked chain-fuel gaps
  closed. `auto`-returning functions now push the same implicit-return
  witnesses Perl bodies do (`SymbolReturnArm` arms, folded by the existing
  generic reducer — zero new reducer code), and free-function chain roots
  (`make_widget().next()`) resolve through the call's own return. Gold
  336→339, the `autoret` xfail promoted, a new `chainroot.cpp` fixture
  gold from birth. Bonus: implicit `this->field` reads (`return inner_;`)
  now chase the field's declared type — general, not return-gated.
- **The review gate worked end-to-end on this one**: the first draft
  embedded cpp semantics in the language-generic extraction tier; a
  mid-flight steer moved it into the driver pipeline's named phase 7 —
  the exact slot the pipeline refactor had reserved. The corrected
  version's skeleton captures are purely structural ("a return happened
  here"); the cpp reading lives where cpp semantics live.
- **The cursor Slot taxonomy is real** (`src/cursor_slot.rs`): one closed
  vocabulary for "what hole is the cursor in" across Perl AND the pack
  languages — completion + sig-help now switch on `Slot`, never on
  language. Migration proven byte-identical on 6 fixture slots (old binary
  vs new, empty diffs). `Slot::expected_type()` landed as the seam
  type-constrained completion plugs into — resolving a local callee's
  param type through the witness bag, locked by unit tests before any
  consumer exists. One honest fork logged (`ModulePath.in_use`).
- **The dogfood loop is now a skill** (`.claude/skills/dogfood-loop/`):
  the whole hardening cycle — corpus probing, grep-sanity, RED-xfail-
  before-fix, root-cause-by-experiment, merge-gate, sweep — written down
  language-agnostically, ready to fire at the next language.
- **`language_driver.rs` got the builder.rs treatment** — the pack analyze
  pipeline is now seven named phases with load-bearing ordering constraints
  documented at the phase list AND each call site (`gather_pack_context` →
  `transform_and_parse` → extract → `remap_spans` → `enrich_skeleton` →
  `into_file_analysis` → `register_post_build`). Pure code motion, zero
  behavior change, full net green. The organic-growth pile is gone.

## Caught & corrected (the review gates earning their keep)

- **Layering leak caught in-flight**: the chain-fuel sonnet agent embedded
  cpp implicit-return semantics inside language-generic `query_extract.rs`.
  Caught at merge-gate review, corrected via mid-flight steer (not a
  post-hoc rewrite) — the emission moved into the freshly-named driver
  pipeline slot that the phase refactor had explicitly reserved for it.
  The two agents' work composed *because* the pipeline got named first.

## Round status

**The tightening round CLOSED green**: pipeline phases, slot taxonomy,
chain fuel, entity-content unification, annotation-priority fix, and the
opus round-close sweep (capability flag landed; the luck-dependent
`len()==1` parents fallback deleted outright with everything green).
Gold 336 → 342 across the round; zero FAIL/XPASS/CRASH throughout;
every merge gated by diff review + the full verification net.

## Landed — the DEPTH round, wave 1 (all three slices, same day)

- **Overload arity ranking**: calls know their arg count, signatures know
  their param arity (total/required/variadic), and the CandidateSet ranks
  the overload family by fit — exact > defaults-compatible > mismatch,
  never pruned. Live proof: abseil's `Substitute` gd's to the 2-, 3-, or
  4-param overload matching each call site. The hardcoded `arity_hint`
  residual died with the same fuel.
- **Parametric macro return**: `ID(w)->poke()` chains through — the macro
  call edges to its argument's own witness (edges-not-values), with
  paren/cast peeling and a discriminating two-param fixture proving it
  selects the RIGHT argument. `ReturnExpr::Arg(n)` is the positional
  sibling of the existing `Receiver` substitution — the model extended
  along its own grain.
- **`InferredType::Bool`**: comparisons, negation, `defined`, cpp `bool`
  decls and comparisons all type Bool; `<=>`/`cmp` correctly stay Numeric;
  Moo `isa => 'Bool'` accessors finally honest. Fold rule documented and
  pinned: Bool ⊔ Numeric = Numeric (Perl truthiness respected).
- Gold **342 → 355** across the wave, both modes, zero
  FAIL/XPASS/CRASH throughout. Two EXTRACT_VERSION collisions (one
  differing-value, one same-value — the sneaky kind) caught and
  reconciled at the merge gate; cache integrity intact at 147.

## Landed — depth round wave 2 (the showpiece)

- **Type-constrained completion is live.** At `o->op_type == |` in REAL
  perl5 op.c, the opcode domain ranks first: `OP_NULL / OP_STUB /
  OP_SCALAR / …` tagged `opcode`, cross-file from the headers, with all
  713 other candidates still present behind them — ranked, never pruned.
  The `Slot::expected_type()` seam got its first two consumers (the pack
  domain-comparison slot + Perl ArgPosition variable ranking), detection
  is pack-declared config (no hardcoded node kinds), and the type gate
  correctly DECLINES incoherent or int-compared fields — the value
  answers, no name checks anywhere. Five born-gold rows. Gold: **360**.
- Depth round total: PARKED's feature tier shrank by four entries in one
  day (overload arity, parametric macro return, Bool, type-constrained
  completion), each with real-corpus proof.

## Dogfood round 3: reported, synthesized, fix wave FIRED

- **Every fix from this session held under independent re-probing**:
  Substitute arity at all 4 arities, join() both ways, GUARDED_BY 56/56
  grep-exact, format_to ~90, pTHX_ bodies across 5 functions, and domain
  completion ranking OP_* in true declaration order at real op.c sites.
  The probes explicitly called the embed.h alias hop ("genuinely nice" —
  gd gives macro def + real function, labeled).
- **~30 raw findings deduped into 4 families**:
  the star is Family Q — five wrong-answer symptoms across three corpora
  (case labels landing on unrelated #defines, level::info resolving into
  bundled dragonbox, members losing to free functions) that all reduce to
  ONE qualifier/owner-blind forward-resolution path, convicted by the
  fact that gr at the same coordinates was correct every single time.
  Family M root-caused SV's total darkness to tree-sitter truncating
  macro bodies at embedded comments — the single most central perl5 type,
  one span-derivation bug away. Plus the C++ twin of the annotation-
  priority bug (braced-init beats declared type) and the missing half of
  implicit-this (sibling calls).
- **All three fix slices LANDED same-day.** Gold finished at **379** —
  up 37 rows today, every one born from a real-corpus failure:
  - **Q**: the qualifier/owner-blind resolver is dead. folly's
    `dynamic::STRING` case label stopped teleporting to a benchmark
    file's unrelated `#define`; spdlog's `level::info` stopped landing
    in bundled dragonbox; members outrank same-named free functions.
    One owner-match rule, five symptoms, zero special cases.
  - **M**: **SV lives.** perl5's central type had zero member
    intelligence because tree-sitter truncates macro bodies at embedded
    comments — plus a second fault (parametric member-block macros
    weren't supported at all). Both fixed; `sv->sv_flags` gd/hover
    verified independently at an active pp.c site. Best part: the
    "BASEOP drops 4 fields" finding was PROVEN a misattribution — the
    probes had landed in `#ifdef DEBUGGING` regions (the known
    superposition tier). The agent dumped the synthesized spans instead
    of trusting the bug report. That's the discipline working.
  - **A+I**: declared types govern braced-init (the C++ twin of the
    annotation bug, killed on the priority axis), and implicit-`this`
    sibling calls link up — including out-of-line template bodies where
    the enclosing class had to come from the peeled symbol, not the
    scope.
- **One incident, contained and hardened**: an agent's stale-base
  self-repair ran a `git reset` that touched the SHARED checkout and
  destroyed an uncommitted scratch edit (sner.pl, 12 bytes — low value,
  pure luck). Branch verified restored; the worktree-agent guard memory
  now mandates non-destructive-only self-repair and a coordinator audit
  whenever an agent report mentions the shared checkout.
- **Round 3 CLOSED** — the sweep removed Q's belt-and-suspenders block
  under proof, folded the macro-nested-ref notes into one pinned entry,
  and recorded duplication verdicts so the next sweep doesn't
  re-litigate. Final state: **gold 379 / 0 / 0 / 0 both modes**, docs
  current, four open forks awaiting your review in docs/open-forks.md.

## The finale: the "cold-start flake" is DEAD, and it was never a flake

- The M6/L3 nondeterminism — your op.c outline blip, my poisoned
  probes, three agents' e2e failures — was a **DashMap shard-reentrancy
  DEADLOCK**: handlers held a FileStore read guard across `resolve()`
  (which re-reads the same shard), a diagnostics-refresh write queued
  between the two reads, and parking_lot's writer preference wedged
  each handler until the tokio pool starved. Timing-dependent, so it
  wore a warmup-race costume for weeks. The agent proved it by noticing
  the handlers never even ENTERED on failing runs.
- Fix: `Document::analysis` is now `Arc<FileAnalysis>` — handlers
  snapshot and DROP the guard; enrichment goes copy-on-write. Repro
  loop: **3/40 cold failures before → 0/40 after**; I re-ran cold cpp
  e2e ×3 independently at the merge gate — all first-try green. The
  repro script is committed (`e2e/cold-start-repro.sh`) as the
  permanent lock, and the residual degraded-window design question is
  honestly PARKED rather than papered over.
- Second deadlock of the arc from the same held-guard family → the
  discipline is now a durable memory + a hazard doc on the API itself.

## The queue when you're back

- Config-superposition-on-DECLARATIONS: now THE dominant remaining
  semantic tier (json's 80% attribution break + BASEOP's config-twin
  fields + per-macro salvage all reduce to it). Wants a design pass
  before implementation — good first discussion for next session.
- Four open forks to ratify (ModulePath.in_use, deref snippets,
  ArgPosition.expected, field/call capability flag naming).
- Round 4 candidates: template rungs (dependent types), flag-set
  domains, the nested-ref gr xfail, workspace-symbol for pack files.

## Round 4: hitlist-4 families, poison-proof, superposition slice 1

- **op.c/op.h dogfood → six findings, four families, all landed.** The
  triage itself was the win: three of the six reproduced ONLY in the
  server's first-open window, so the honest root-cause split them into a
  degraded-window family (B) and three real core gaps (A/C/D) — no thrash
  fixing symptoms that were already warm-correct.
  - **A — the `OP` gd hijack, your top pain, is dead.** gd on the `OP`
    type teleported to `#define OP(p) ((p)->head.data.type)` (a scoped
    regex-internal macro) instead of `typedef struct op OP`. Fixed at the
    shape, not the name: a function-like macro expands ONLY before `(` —
    C's own rule — so a parenless `OP**` in type position can't be that
    macro, and the typedef wins the candidate lane. Zero name allowlist.
  - **C — `op_next` hovers `OP*`, not `OP`.** A field declared inside a
    `#define BASEOP` body now mints as a `Field` of `op` carrying its full
    payload: the deref stars survive, the owner is the class not the macro
    package, and def-site/use-site hover finally agree.
  - **D — the DEEP-receiver peel hint has a producer.** `(*op_p)->` ("you
    need to peel, not swap the operator") now emits, and Mode-B member
    diagnostics reach the CLI (`--batch`/`--check`) so gold locks the same
    answers the LSP publishes.
  - **B — the first-open degraded window heals server-side.** The
    heal-repush re-analyzes open docs when the index lands (refs 1→118 on
    op.c open), and the resolver refresh coalesces 33 fires → 1. The
    residual bounded-wait is honestly ledgered, not papered.
- **Poisoned-persist is proven non-sticky.** The worry was a degraded
  cold-run analysis freezing into the SQLite pack cache behind a
  self-validating stamp. Two guards close it (refuse to persist a degraded
  analysis; fold include-closure INCOMPLETENESS into degraded), and the
  proof is causal: under heavy CPU load every persisted blob is the full
  analysis, and a warm run heals the transient window WITHOUT
  `--clear-cache` — a genuine poison would fail every warm run.
  `e2e/persist-poison-repro.sh` is the lock.
- **Superposition slice 1 — and the honest Case-A re-diagnosis.** The
  declaration-position `#if` directive repair landed (no phantom
  `start_position`/`end_position` members from an isolated ctor-init
  `#if`). But the slice's real value was proving what DIDN'T fix json.hpp:
  the ctor `#if` in isolation causes only LOCAL damage — the 4400-line
  `basic_json` attribution break is deep-error-propagation (the class
  degrades to ERROR soup, no scope to attribute to), a parse corruption,
  NOT a config superposition. Slice 2 (variant tags) was re-scoped OUT of
  Case A on the evidence rather than thrown at it.
- **heatmap lit up for C/C++.** `--heatmap` now covers pack languages —
  per-symbol fan-in/fan-out over the pack sub-indexes (fan-in routes
  through each pack's own cache, matching the references verb by
  construction), a real usage/dead-code view for a cpp tree.
- **The diagnostics round merged with honest cpp parking.** The narrowing
  D-codes stay Perl-only for now — their fact seams are minted by the
  Perl-only tree-sitter consumer, and cpp goes through `query_extract` —
  so the cpp applicability is documented (`docs/adr/narrowing-diagnostics.md`)
  and PARKED with the exact unblock (a cpp nullability pass), not faked.
- **Gold 379 → 407**, both modes cold, zero FAIL/XPASS/CRASH throughout;
  every merge gated by diff review + the full net.
